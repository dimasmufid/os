# @os/ui

Shared UI components used across the monorepo.

## Usage

Import components in your apps:

```typescript
import { Button } from "@os/ui";
```

## Development

This package contains reusable React components that can be shared between the web app and potentially other frontend applications.

